---
import type { PageMeta } from '@stephenchenorg/astro/page'
import { seoMeta } from '@stephenchenorg/astro/page'
import { getLocale } from 'i18n:astro'
import { getHomepageData } from '@/api/homepage'
import HeroBanner from '@/components/HeroBanner.astro'
import IndexNews from '@/components/IndexNews.astro'
import ServiceSection from '@/components/ServiceSection.astro'
import TeamSection from '@/components/TeamSection.astro'
import Layout from '@/layouts/Layout.astro'
import { log } from '@/utils/log'
import { getCanonicalUrl, getSeoConfig } from '@/utils/seoConfig'
import IntroSection from '../components/IntroSection.astro'

const currentLocale = getLocale()
const locale = currentLocale === 'en' ? 'en' : 'zh_TW'

// 單一批次查詢取得首頁所有資料，進一步提升載入速度
const startTime = performance.now()
const homepageData = await getHomepageData()
const totalTime = performance.now() - startTime
log(`🚀 Total API time (batched single query): ${totalTime.toFixed(2)}ms`)

// 處理資料
const { banners, teams, serviceCategories, articles: allArticles, page } = homepageData
const teamMembers = teams.slice(0, 6)
const services = serviceCategories.serviceCategories.data
const articles = allArticles.slice(0, 6)

// 取得 SEO 設定
const defaultSeo = getSeoConfig('home', locale)

const meta = {
  ...seoMeta({
    title: page.seo_title || defaultSeo.seo_title,
    seo_description: page.seo_description || defaultSeo.seo_description,
    seo_keyword: page.seo_keyword || defaultSeo.seo_keyword,
    og_title: page.og_title || defaultSeo.og_title,
    og_description: page.og_description || defaultSeo.og_description,
    og_image: page.og_image || defaultSeo.og_image,
    // 後台額外欄位 (有值才顯示)
    seo_head: page.seo_head,
    seo_body: page.seo_body,
    seo_json_ld: page.seo_json_ld,
  }),
  canonical: getCanonicalUrl('', locale),
} as PageMeta
---

<Layout meta={meta}>
  <!-- Hero Banner -->
  <HeroBanner banners={banners} />
  <IntroSection />

  <div class="animate-in">
    <TeamSection teamMembers={teamMembers} />
  </div>

  <div class="animate-in">
    <ServiceSection services={services} />
  </div>

  <div class="animate-in">
    <IndexNews articles={articles} />
  </div>

  <script>
    // 等待 DOM 載入完成
    document.addEventListener('DOMContentLoaded', () => {
      // 堆疊卡片效果
      const stackCards = document.querySelectorAll('.stack-card') as NodeListOf<HTMLElement>

      const stackObserver = new IntersectionObserver(entries => {
        entries.forEach((entry, _index) => {
          if (entry.isIntersecting) {
            entry.target.classList.add('active')
          } else {
            entry.target.classList.remove('active')
          }
        })
      }, {
        threshold: 0.3,
        rootMargin: '-50px 0px -50px 0px',
      })

      stackCards.forEach(card => stackObserver.observe(card))

      // 平滑滾動效果
      const scrollLinks = document.querySelectorAll('a[href^="#"]') as NodeListOf<HTMLAnchorElement>
      scrollLinks.forEach(anchor => {
        anchor.addEventListener('click', function (e: Event) {
          e.preventDefault()
          const href = (this as HTMLAnchorElement).getAttribute('href')
          const target = href ? document.querySelector(href) : null
          if (target) {
            target.scrollIntoView({
              behavior: 'smooth',
              block: 'start',
            })
          }
        })
      })

      // 視差滾動效果
      window.addEventListener('scroll', () => {
        const scrolled = window.pageYOffset
        const heroImage = document.querySelector('.hero-image') as HTMLElement
        if (heroImage) {
          heroImage.style.transform = `translateY(${scrolled * 0.5}px)`
        }
      })
    })
  </script>
</Layout>
